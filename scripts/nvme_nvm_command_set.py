"""Independent, source-located NVM Command Set 1.3 course (paired editorial source)."""
from __future__ import annotations

REPORT_ID = 'nvm-command-set-1.3'
PREFIX = 'NVMCS13'
NVM = 'NVME-NVM-CS-1.3'
BASE = 'NVME-BASE-2.4'
LESSONS = []


def b(zh, en):
    return {'zh': zh, 'en': en}


def row(label, zh, en, condition_zh, condition_en):
    return b([label, zh, condition_zh], [label, en, condition_en])


def lesson(key, title, section, pages, claim, lead, rows, example, pitfall, figures, nodes):
    LESSONS.append(dict(key=key, title=title, section=section, pages=pages, claim=claim,
                        lead=lead, rows=b([r['zh'] for r in rows], [r['en'] for r in rows]),
                        example=example, pitfall=pitfall, figures=figures, nodes=b(nodes.split('|'), nodes.split('|'))))


lesson('FOUNDATION', b('閱讀地圖與單位', 'Reading map and units'), '1.1-1.6; 4.1.3.9; 4.1.4.8; 4.1.5', '9-12,73-75,79-83',
 b('NVM Command Set 補充 Base 的 logical-block 語意；logical block data size 不含 metadata，logical block size 則包含。NVM 的 CSI 是 00h，命令、Feature、log 與 Identify 的相同數字分屬不同識別空間。', 'The NVM Command Set supplements Base with logical-block semantics. Logical block data size excludes metadata, whereas logical block size includes it. NVM uses CSI 00h; equal numeric values in commands, Features, logs, and Identify belong to different identifier spaces.'),
 b('本份從 namespace 與資料格式一路走到命令完成、資料完整性及管理證據。主線可分四堂各約 25 分鐘；逐圖附錄與問答供課後查詢。章節 1 的名詞與引用慣例也是範圍的一部分。', 'This course follows namespaces and formats through command completion, integrity, and management evidence. Use four sessions of roughly 25 minutes for the main path, with figure worksheets and questions for further study. Chapter 1 terminology and reference conventions are included.'),
 [row('LBADS', 'data bytes = 2^LBADS', 'data bytes = 2^LBADS', '另加 MS 才是 logical block size', 'Add MS to obtain logical block size'), row('Format Index', '同時選 LBAF 與 ELBAF', 'Selects both LBAF and ELBAF', '不能只看資料大小', 'Data size alone is insufficient'), row('Specification family', '通用機制由 Base 定義', 'Base defines common mechanisms', '相依欄位另以 Base 來源標示', 'Dependencies carry separate Base citations')],
 b('LBADS=0Ch、MS=16 時，資料為 4096 bytes，含 metadata 的 logical block 是 4112 bytes。FID 28h 與 LID 28h 雖相同，前者設定限制，後者回報能力圖。', 'With LBADS=0Ch and MS=16, data occupies 4096 bytes and the complete logical block 4112 bytes. FID 28h configures limits while LID 28h reports the capability graph, despite their equal numbers.'),
 b('本報告的算例是教學推導；不把示意數值寫成裝置必須具備的能力。對規格的 should、may 與 shall 保留不同強度。', 'Worked values are teaching examples, not required device capabilities. Keep the different strengths of should, may, and shall.'), [1,2], 'Namespace|Format|Command|Completion|Evidence')

lesson('CAPACITY', b('Namespace 容量與配置狀態', 'Namespace capacity and allocation'), '2.1.1; 4.1.5.1', '13-14,85-93',
 b('NSZE ≥ NCAP ≥ NUSE；NSZE 定義可定址範圍，NCAP 限制同時配置的 blocks，NUSE 計算目前已配置 blocks。THINP=0 時 NCAP=NSZE；NVMCAP 以 bytes 計，不能直接當成 NSZE 乘資料大小。', 'NSZE ≥ NCAP ≥ NUSE: NSZE defines the addressable range, NCAP limits simultaneously allocated blocks, and NUSE counts current allocation. THINP=0 requires NCAP=NSZE. NVMCAP is measured in bytes and is not necessarily NSZE multiplied by data size.'),
 b('先區分 logical address space 與實際配置，再分析寫入及 deallocate。讀取值與 allocation 狀態回答不同問題。', 'Separate logical address space from allocated resources before considering writes and deallocation. Returned data and allocation state answer different questions.'),
 [row('NSZE', '有效 LBA 為 0 到 NSZE−1', 'Valid LBAs run from 0 to NSZE−1', 'LBA 越界與容量不足不同', 'Out-of-range differs from capacity exhaustion'), row('THINP', '支援時須追蹤 NUSE', 'Requires NUSE tracking when supported', '不支援時可固定回 NCAP', 'Without support NUSE may remain NCAP'), row('Allocation', 'Write、Copy 寫入端及 WU 可配置', 'Write, the Copy destination, and WU allocate', 'Read／Verify 不改 deallocation 狀態', 'Read/Verify do not change deallocation state')],
 b('NSZE=1000、NCAP=800、NUSE=600 可是合法 thin namespace。LBA 900 在可定址範圍內，但新增配置仍受 800-block capacity 限制。', 'NSZE=1000, NCAP=800, NUSE=600 can describe a valid thin namespace. LBA 900 is addressable, but additional allocation remains subject to the 800-block capacity.'),
 b('ANA 狀態可使 NUSE／NVMCAP 回零；不可僅憑回零宣告資料已刪除。', 'ANA state can zero reported NUSE/NVMCAP; those zeros alone do not establish data deletion.'), [123], 'NSID|NSZE|NCAP|NUSE|Allocation')

lesson('ORDER-FUSED', b('命令順序與 Compare-and-Write', 'Ordering and Compare-and-Write'), '2.1.2-2.1.3', '14-15',
 b('一般命令不因同在一個 SQ 就取得 LBA 相依順序；host 必須建立必要順序。Fused Compare-and-Write 先比對，成功才寫入；Compare 失敗則 Write 以 Failed Fused Command 類別中止。', 'Ordinary commands do not gain LBA dependency ordering merely by sharing an SQ; the host enforces required order. Fused Compare-and-Write compares first and writes only on success; a failed Compare aborts Write with the failed-fused-command status.'),
 b('先說明何時要排序，再判斷是否需要條件式更新。Fused 保護同一 LBA range 的比對與更新，原子大小仍須另外檢查。', 'First establish the ordering requirement, then decide whether conditional updating is needed. A fused operation protects comparison and update of the same LBA range, with atomic-size limits checked separately.'),
 [row('Ordinary I/O', '同 LBA 的 Read／Write 完成先後無保證', 'Read/Write completion order for one LBA is not guaranteed', 'host 以完成相依控制', 'Use host completion dependencies'), row('Fused pair', 'Compare 與 Write 的 range 相同', 'Compare and Write use the same range', '範圍不符 should 拒絕', 'A mismatch should be rejected'), row('ACWU / NACWU', '限制 fused atomic update 大小', 'Bounds the fused atomic update', '還要遵守 atomic boundaries', 'Atomic boundaries also apply')],
 b('Host 想「目前值等於 A 才更新 B」時，獨立 Compare 成功後再送 Write 中間仍可能插入別人的寫入；符合大小與邊界的 fused pair 才提供此操作所需的條件式原子更新。', 'For “replace A with B only if A is still present,” an independent Compare followed by Write permits an intervening write. A fused pair within its size and boundary limits provides the conditional atomic update.'),
 b('Write 失敗不會回頭改寫 Compare 已得到的 completion status；必須檢查兩個 CQE。', 'Write failure does not retroactively change the Compare completion status; inspect both CQEs.'), [3], 'Host dependency|Compare|Match gate|Write|Two CQEs')

lesson('ATOMIC', b('正常、斷電與多段原子性', 'Normal, power-fail, and multiple atomicity'), '2.1.4; 4.1.3.4; 5.9', '15-21,66-67,165',
 b('AWUN／NAWUN 與 AWUPF／NAWUPF 分別描述正常及失敗條件原子性。Single Atomicity Mode 跨 boundary 不保證整筆原子；Multiple Atomicity Mode 在每個 boundary 切成各自原子的 subranges，並不承諾整筆一起成功。', 'AWUN/NAWUN and AWUPF/NAWUPF describe normal and failure-condition atomicity. Single Atomicity Mode provides no whole-command guarantee across a boundary; Multiple Atomicity Mode divides the range at boundaries into separately atomic subranges, without promising one combined outcome.'),
 b('把大小、起始對齊、NSABP 與 MAM 一起讀。Atomicity 與資料已進入 nonvolatile media 是不同檢查項目，FUA／Flush 也不建立其他命令的排序。', 'Read size, starting alignment, NSABP, and MAM together. Atomicity and persistence on nonvolatile media are separate checks; FUA/Flush do not establish ordering for other commands.'),
 [row('AWUN / AWUPF', '大小採 0-based 編碼', 'Sizes use zero-based encoding', 'AWUPF 不大於 AWUN', 'AWUPF does not exceed AWUN'), row('NABO / NABSN / NABSPF', '邊界在 offset + k × size', 'Boundaries occur at offset + k × size', '需依各欄位解碼與未回報規則', 'Decode each field and its unreported cases'), row('MAM', '每個 atomic subrange 獨立保證', 'Each atomic subrange has its own guarantee', 'fused 仍用 Single 模式', 'Fused operations still use Single mode'), row('FID 0Ah.DN', 'DN=1 可不遵守 normal atomicity', 'DN=1 releases normal-atomicity requirements', '仍須遵守 power-fail 保證', 'Power-fail guarantees remain')],
 b('假設解碼後 boundary size=8 blocks、offset=0，從 LBA 4 寫 12 blocks 跨成 [4..7] 與 [8..15]。MAM 下各段原子，不能將兩段當作一個 transaction。原始 AWUN=7h 才代表 8 blocks。', 'For decoded boundary size 8 blocks and offset zero, a 12-block write starting at LBA 4 splits into [4..7] and [8..15]. Under MAM each part is atomic, not one transaction. Raw AWUN=7h represents eight blocks.'),
 b('不要把 §3.3.2.2 範例的「NAWUN=8h 表示 8 blocks」照抄；0-based 欄位與示意的已解碼大小必須區分。寫入失敗也可能完成 DMA，不能用 buffer 傳輸完成推論媒體成功。', 'Do not copy the §3.3.2.2 example equating NAWUN=8h with eight blocks: distinguish raw zero-based fields from decoded example sizes. Failed writes may still complete DMA, so buffer transfer completion is not media success.'), list(range(4,11)), 'NSABP|Decode size|Check boundaries|MAM or single|Failure outcome')

lesson('SUPPORT-STATUS', b('能力探索、Opcode 與狀態', 'Capabilities, opcodes, and status'), '2.2; 3.1; 3.3', '22-27',
 b('NVM I/O controller 必須支援 Read 與 Write；其他列出的 NVM 命令依各能力條件判斷。Opcode 低兩 bits 表示資料傳輸方向，狀態值必須連同 SCT 解讀，不能只用 SC 數值查錯誤。', 'An NVM I/O controller must support Read and Write; other listed NVM commands depend on their capability conditions. The low two opcode bits identify transfer direction. Interpret status values with SCT rather than looking up SC alone.'),
 b('從 controller 類型開始建立命令、Feature、log 的能力矩陣。資料指標指向的可能是使用者資料，也可能只是控制 descriptor。', 'Build a command/Feature/log capability matrix starting from controller type. A data pointer may reference user data or merely control descriptors.'),
 [row('Read 02h / Write 01h', '必要 I/O 命令', 'Mandatory I/O commands', 'Administrative controller 不處理 I/O', 'Administrative controllers do not process I/O'), row('Get LBA Status 86h', '選用 Admin 命令', 'Optional Admin command', 'I/O controller 適用能力', 'Capability belongs to I/O controllers'), row('SC 80h', '依 SCT 區分 LBA Out of Range 等', 'SCT distinguishes LBA Out of Range and other meanings', '同時記錄 opcode、NSID、SCT、SC、DNR', 'Record opcode, NSID, SCT, SC, and DNR'), row('FID / LID', '05h、0Ah 為必要 NVM Features', '05h and 0Ah are mandatory NVM Features', '功能支援不等於要求寫入 Persistent Event Log', 'Feature support differs from Persistent Event Log recording')],
 b('Copy opcode=19h 的低 bits 是 01b，因 host 傳入 source descriptors；並不表示需要把整份待複製資料從 host 再傳一次。', 'Copy opcode 19h has low bits 01b because the host supplies source descriptors; it does not require the host to retransmit all user data being copied.'),
 b('FFFF FFFFh NSID 不是所有命令都接受；Figure 22 只有特別註明的 Flush／Cancel 路徑可支援。', 'NSID FFFF FFFFh is not accepted by every command; Figure 22 permits it only for the specifically noted Flush/Cancel paths.'), list(range(13,21))+[22], 'Controller type|Capability|Opcode and NSID|SCT plus SC|Recovery')

lesson('READ-WRITE', b('Read／Write 的資料與完成條件', 'Read/Write data and completion'), '3.3.4; 3.3.6', '48-51,53-56',
 b('Read／Write 以 SLBA 與 0-based NLB 指定連續範圍。Read 的 DPTR 是目的 buffer，Write 的 DPTR 是來源 buffer；FUA=1 要求使用 nonvolatile media，並沒有隱含其他命令的順序。', 'Read/Write specify a contiguous range with SLBA and zero-based NLB. DPTR is a destination buffer for Read and a source buffer for Write. FUA=1 requires nonvolatile-media handling without implying ordering of other commands.'),
 b('把範圍、buffer、PI 與完成狀態分成四項檢查，才能分辨資料位址錯誤、格式不符與真正的媒體失敗。', 'Check range, buffer, PI, and completion separately to distinguish addressing errors, format mismatches, and media failures.'),
 [row('CDW10 / CDW11', 'SLBA 低／高 32 bits', 'Low/high 32 bits of SLBA', 'NLB=0 仍有一個 block', 'NLB=0 still requests one block'), row('CDW12', 'LR、FUA、PRINFO、STC、CETYPE、NLB', 'LR, FUA, PRINFO, STC, CETYPE, NLB', 'Read 的 DTYPE 區是 reserved', 'The DTYPE area is reserved for Read'), row('CDW13', 'CETYPE 決定 DSM 或 CEV 解讀', 'CETYPE selects DSM or CEV interpretation', 'Write 另含 DTYPE／DSPEC', 'Write also carries DTYPE/DSPEC'), row('MPTR', '單獨傳 metadata 時使用', 'Used for separate metadata', '不可把一部分 metadata 分到兩種機制', 'Do not split metadata between both mechanisms')],
 b('SLBA=100、NLB=7、LBADS=12 且無 metadata：讀取 100..107，共 32768 bytes。FUA Read 先使對應資料持久化，再取自媒體；仍需 host 保證相依 Write 的排序。', 'With SLBA=100, NLB=7, LBADS=12, and no metadata, Read covers 100..107 and 32768 bytes. An FUA Read commits the corresponding data before retrieving it from media; the host still orders dependent Writes.'),
 b('CQE 失敗時保存 SCT／SC 與 range，不能用 FUA 或成功 DMA 取代 CQE 結果。CETYPE 非零時也不能將 CDW13 低 bits 當作 DSM hints。', 'On CQE failure retain SCT/SC and the range; FUA or successful DMA does not replace the completion result. With nonzero CETYPE, low CDW13 bits cannot be decoded as DSM hints.'), list(range(50,60))+list(range(67,77)), 'Range|Buffer layout|PI and FUA|Execute|CQE')

lesson('COMPARE-VERIFY', b('Compare 與 Verify 解決不同問題', 'Compare and Verify answer different questions'), '3.3.1; 3.3.5', '27-30,51-53',
 b('Compare 比較媒體資料與 host 提供的 buffer；Verify 檢查已儲存資料完整性而不把資料或 metadata 回傳 host。兩者要求 PRACT=0；Verify 與 Read 偵測到的失敗不必使用相同錯誤碼。', 'Compare compares media data with a host-supplied buffer. Verify checks stored-data integrity without returning data or metadata to the host. Both require PRACT=0; Verify and Read need not report an identical error code for a detected failure.'),
 b('比對預期內容、驗證完整性與一般 Read 三者的證據不同。Compare 的 metadata 比對排除 PI；PI 另依要求的 checking 執行。', 'Expected-content comparison, integrity verification, and ordinary Read provide different evidence. Compare excludes PI from metadata comparison and checks PI separately as requested.'),
 [row('Compare', 'miscompare 回 Compare Failure', 'A miscompare returns Compare Failure', 'host 與 media 兩側 PI 可分別檢查', 'PI can be checked on both host and media paths'), row('Verify', '沒有資料 buffer 傳輸', 'No data-buffer transfer', '驗證量仍計入 Data Units Read', 'Verified data still counts toward Data Units Read'), row('VSL / NVMVFYS', 'variant 決定建議大小或硬上限', 'The variant selects recommended size or a hard limit', '非零 VSL 以 2^n × minimum page size 表示', 'Nonzero VSL uses 2^n × minimum page size')],
 b('要知道儲存內容是否等於全零，Compare 可提供預期全零 buffer；Verify 成功只能證明此次完整性檢查成功，不能證明內容是全零。', 'To determine whether stored contents equal zero, Compare can use a zero-filled expected buffer. Successful Verify establishes this integrity check, not that the contents equal zero.'),
 b('Verify 指定 PRACT=1 會回 Invalid Field in Command。把 Verify 寫成零長度 Read 或把 Compare 當成傳出資料都會錯誤配置 buffer。', 'Verify with PRACT=1 returns Invalid Field in Command. Treating Verify as a zero-length Read or Compare as a data-return command leads to incorrect buffer setup.'), list(range(23,32))+list(range(60,67)), 'Question|Compare or Verify|PRACT and checks|Size gate|Status')

lesson('COPY', b('Copy：描述來源、連續目的與部分失敗', 'Copy: source descriptors, contiguous destination, partial failure'), '3.3.2', '30-44',
 b('Copy 把一個或多個 source ranges 依 descriptor 順序接成單一連續目的範圍。Format 0h/1h 來源與目的在同一 namespace；2h/3h 帶 SNSID，需 controller 支援與 host 啟用。失敗 CQE DW0 是最低未成功 source index，後面的 ranges 仍可能已複製。', 'Copy concatenates one or more source ranges in descriptor order into one contiguous destination range. Formats 0h/1h use one namespace; 2h/3h carry SNSID and require controller support and host enablement. Failure CQE DW0 is the lowest unsuccessful source index; later ranges may already have been copied.'),
 b('先計算展開後的目的區間，再檢查格式、長度限制、重疊與 atomicity。Copy 可少用 host 資料傳輸，但不是無條件的 transaction。', 'Calculate the expanded destination range before checking formats, limits, overlap, and atomicity. Copy reduces host data movement but is not an unconditional transaction.'),
 [row('NR / NLB', 'source count 與各範圍 block count 都是 0-based', 'Source count and per-range block count are zero-based', '檢查 MSRC、MSSRL、MCL', 'Check MSRC, MSSRL, and MCL'), row('FCO', '2h/3h 可要求 fast copy only', 'Formats 2h/3h can request fast copy only', 'Fast Copy Not Possible 再看 DNR', 'Inspect DNR after Fast Copy Not Possible'), row('Overlap', '2h/3h 禁止同 namespace source 與 destination 重疊', 'Formats 2h/3h prohibit source/destination overlap within one namespace', '0h/1h 重疊結果需依原子條件另讀', 'Formats 0h/1h need their separate overlap rules'), row('NVMCSA', '1.3 將目的寫入視為單一 write command', 'Revision 1.3 treats the destination as one write command', '仍受 MAM、大小及 boundary 限制', 'MAM, size, and boundary limits still apply')],
 b('兩個 source descriptors 的 NLB 都是 3，SDLBA=100：第一段寫 100..103、第二段寫 104..107，共 8 blocks。若 DW0=1，不能據此保證第二段或更後面完全未修改。', 'Two descriptors with NLB=3 and SDLBA=100 write 100..103 and 104..107: eight blocks total. DW0=1 does not prove that the second or any later range is entirely unmodified.'),
 b('FCO 是對方法的限制，非每次 latency 保證。原子欄位 FFFFh 最多表示 65536 blocks，Copy 能超過此大小，不能據此宣告所有 Copy 都原子。', 'FCO constrains the method, not the latency of every operation. Atomicity value FFFFh represents 65536 blocks; Copy can exceed that size, so it does not make all Copy commands atomic.'), list(range(32,44)), 'Descriptors|Destination range|Limits and overlap|Copy|Partial-result CQE')

lesson('COPY-PI', b('Copy 的 PI 格式相容與轉換', 'Copy PI compatibility and transformation'), '3.3.2.3-3.3.2.4; 5.3.2.5', '40-43,146-150',
 b('Copy 的 matching formats 要比對資料大小、metadata 大小、DPS、PIFA、有效 LBSTM、PIF／QPIF 與 STS。Corresponding PI formats 只容許有 PI 的一方 metadata 全為 PI，另一方完全沒有 metadata；不能拿 Copy 任意轉換資料格式。', 'Matching Copy formats compare data size, metadata size, DPS, PIFA, significant LBSTM bits, PIF/QPIF, and STS. Corresponding PI formats require PI-only metadata on one side and no metadata on the other; Copy is not a general format converter.'),
 b('以來源與目的是否有 PI 建立四種情況，再決定 PRINFOR.PRACT 與 PRINFOW.PRACT。每個 source 都必須符合選定模式。', 'Classify whether source and destination use PI, then choose PRINFOR.PRACT and PRINFOW.PRACT. Every source must fit the selected mode.'),
 [row('PI → PI, 0/0', 'matching formats：pass-through', 'Matching formats: pass-through', '保護檢查仍由 checking bits 控制', 'Checking remains controlled by check bits'), row('PI → PI, 1/1', 'matching formats：replace', 'Matching formats: replace', '讀端檢查，寫端產生 PI', 'Check the read path and generate write PI'), row('No PI → PI', 'corresponding formats 且 write PRACT=1：insert', 'Corresponding formats and write PRACT=1: insert', '目的 metadata 不得包含其他用途', 'Destination metadata cannot include other content'), row('PI → No PI', 'corresponding formats 且 read PRACT=1：strip', 'Corresponding formats and read PRACT=1: strip', '來源 metadata 只能是 PI', 'Source metadata must be PI-only')],
 b('4096+8 bytes 的 16b PI namespace 可在符合 corresponding 條件時轉到 4096+0 bytes；4096+16 bytes 且其中 8 bytes 是額外 metadata，不能走這個 strip 特例。', 'A 4096+8-byte namespace with 16b PI can copy to 4096+0 bytes under the corresponding-format conditions. A 4096+16-byte source with eight extra metadata bytes cannot use that strip exception.'),
 b('§5.3.2.5.1／.2 將 Figures 177–180 的引用對調；以各圖實際標題與 §3.3.2.4.2 的 0/0 pass-through、1/1 replace 規則交叉核對。', 'Sections 5.3.2.5.1/.2 interchange references to Figures 177–180. Cross-check their actual captions with §3.3.2.4.2: 0/0 passes PI through and 1/1 replaces it.'), list(range(177,183)), 'Source PI|Destination PI|Matching or corresponding|PRACT pair|Check and transform')

lesson('DSM', b('Dataset Management 與三種 processing limits', 'Dataset Management and three processing limits'), '3.3.3', '44-48',
 b('Dataset Management 是 advisory：處理 attributes 不等於一定執行 deallocate。NR 是 0-based，16-byte descriptor 的 LLB 是 1-based；DMRL、DMRSL、DMSL 分別約束 range 數、單一 range 長度與總長度。', 'Dataset Management is advisory: processing attributes does not guarantee deallocation. NR is zero-based; LLB in each 16-byte descriptor is one-based. DMRL, DMRSL, and DMSL constrain range count, individual range length, and total length.'),
 b('先計算每個 block 是否同時通過三種 limit，再套用 NVMDSMSV。範圍與 hints 的合法性、處理義務以及實際媒體動作分開判斷。', 'Determine whether each block satisfies all three limits before applying NVMDSMSV. Separate range/hint validity, attribute processing obligations, and actual media actions.'),
 [row('AD / IDW / IDR', 'deallocate／整體寫入／整體讀取 hints', 'Deallocate/integral-write/integral-read hints', '可組合使用', 'Combinations are permitted'), row('Limits nonzero, variant=0', '超過任一 limit 回 Command Size Limit Exceeded', 'Exceeding a limit returns Command Size Limit Exceeded', '全部符合則須處理 attributes', 'All fitting blocks require attribute processing'), row('Limits nonzero, variant=1', '宜處理符合 limits 的部分', 'Should process the portion satisfying the limits', '不以此原因回 Size Limit Exceeded', 'Do not report Size Limit Exceeded for this reason'), row('All limits=0', 'variant=1：不回報 limits；variant=0：不支援', 'Variant=1: no limits reported; variant=0: unsupported', '三欄需全零或全非零', 'All three fields must be zero or all nonzero')],
 b('DMRL=2、DMSL=1048，range 0 有 1024 blocks、range 1 有 512，且 DMRSL 不限制：variant=1 時前段與後段前 24 blocks 符合 limits。這不等於保證 1048 blocks 都已釋放。', 'With DMRL=2, DMSL=1048, ranges of 1024 and 512 blocks, and a nonrestrictive DMRSL, variant=1 admits the first range and 24 blocks of the second. This does not guarantee deallocation of 1048 blocks.'),
 b('不要把 Write 的 NLB encoder 套到 DSM 的 LLB。以 0-based 寫入 LLB 會少描述一個 block；LLB=0 也不代表一個 block。', 'Do not reuse the Write NLB encoder for DSM LLB. Zero-based encoding undercounts the range by one; LLB=0 does not mean one block.'), list(range(44,50)), '16-byte ranges|Three limits|NVMDSMSV|Process hints|Allocation evidence')

lesson('DEALLOC', b('Deallocated／unwritten 讀取規則', 'Reading deallocated/unwritten blocks'), '3.3.3.2.1; 4.1.3.3', '47-48,66',
 b('支援且啟用 DULBE 時，Copy、Read、Verify、Compare 存取 deallocated／unwritten blocks 會失敗。未啟用時，DRB=001b 回零、010b 回 FFh、000b 可選其一；同 block 在下一次寫入前的回值須保持 deterministic。', 'When supported and enabled, DULBE makes Copy, Read, Verify, and Compare fail on deallocated/unwritten blocks. Otherwise DRB=001b returns zeros, 010b returns FFh, and 000b allows either; a block must return a deterministic value until its next write.'),
 b('先判斷是否允許成功讀取，再解釋成功回傳的 bytes。Allocation status、DRB 與 PI 有各自的條件。', 'First determine whether successful reading is permitted, then interpret returned bytes. Allocation status, DRB, and PI have separate conditions.'),
 [row('DAE / DULBE', 'DAE 是 capability，DULBE 是啟用', 'DAE is capability; DULBE is enablement', 'DULBE 預設 0', 'DULBE defaults to zero'), row('DRB=000b', '不是任意舊資料', 'Does not mean arbitrary old data', '依 §3.3.3.2.1 為零或 FFh', 'Section 3.3.3.2.1 limits it to zeros or FFh'), row('PI after deallocation', 'tag bytes 回 FFh；Guard 為 FFh 或 CRC', 'Tag bytes return FFh; Guard is FFh or CRC', '配合 DLFEAT.GDS', 'Use DLFEAT.GDS')],
 b('一次 Read 全零不能證明 sanitize 成功，可能只是 deallocated 的 DRB。反過來 DULBE 啟用後讀取報錯，也不能單凭此錯誤認定媒體故障。', 'One zero-filled Read does not prove successful sanitize; it may reflect DRB on deallocated blocks. Conversely, a DULBE error alone does not establish a media defect.'),
 b('Read／Verify 成功不會使該 block 變回 allocated；重新寫入才會改變這個狀態。', 'Successful Read/Verify does not reallocate the block; writing changes that state.'), [], 'DAE|DULBE|DRB|PI values|Deterministic read')

lesson('ZERO-UNCORRECTABLE', b('Write Uncorrectable、Write Zeroes 與整體清零', 'Write Uncorrectable, Write Zeroes, and whole-namespace zeroing'), '3.3.7-3.3.8', '56-61',
 b('Write Uncorrectable 將範圍標為無法修復的讀取錯誤；Write Zeroes 使成功的後續讀取資料及非 PI metadata 回零。NSZ=1 的整個 namespace 清零需 NSZS、DEAC=1 與零值 deallocation read behavior，且 host 應檢查 CQE.LBACZ。', 'Write Uncorrectable marks a range for unrecovered-read errors. Write Zeroes makes subsequent successful reads return zero data and non-PI metadata. Whole-namespace zeroing with NSZ=1 requires NSZS, DEAC=1, and zero-valued deallocation reads; the host should check CQE.LBACZ.'),
 b('這兩個命令沒有 host user-data payload，但會改變 namespace 語意。先辨識範圍模式，再分配 PI 與 size-limit 檢查。', 'Neither command transfers a host user-data payload, but both change namespace semantics. Identify range mode before checking PI and size limits.'),
 [row('Write Uncorrectable', '標記 block 後，讀取可能報 Unrecovered Read Error', 'Marks blocks so later reads can report Unrecovered Read Error', 'WUSL 與 NVMWUSV 需成對看', 'Interpret WUSL with NVMWUSV'), row('Write Zeroes PI', 'PRCHK=000b、STC=0', 'PRCHK=000b, STC=0', 'PRACT=1 宜用於產生有效 PI', 'PRACT=1 is recommended for valid PI'), row('WZSL / WZDSL', '依 DEAC 選適用 limit', 'Select the applicable limit using DEAC', 'NSZ=1 不受這兩欄限制', 'Neither limit applies with NSZ=1'), row('LBACZ', '成功 NSZ 命令回 1 才表示全 namespace', 'One on a successful NSZ command reports the entire namespace', '回 0 是指定 range', 'Zero reports the specified range')],
 b('Host 送 NSZ=1 後得到 Successful Completion 與 LBACZ=0，必須判為僅 range 被清零；舊 controller 可能忽略不支援的 NSZ。不能將 command success 自動升格為整個 namespace 清零。', 'Successful Completion with NSZ=1 but LBACZ=0 means only the range was zeroed. An older controller may ignore unsupported NSZ; command success alone does not establish whole-namespace zeroing.'),
 b('DEAC=0 不是禁止 deallocate：只要 zero-read 條件成立，controller 仍 may deallocate。失敗的 Write Zeroes 也可能已清掉部分內容。', 'DEAC=0 does not prohibit deallocation: the controller may still deallocate when zero-read conditions hold. A failed Write Zeroes can also have cleared some contents.'), list(range(77,90)), 'Operation|Range or NSZ|PI and limits|Execute|LBACZ and status')

lesson('FORMAT', b('Format、Host Behavior 與延伸 LBA', 'Format, Host Behavior, and extended LBAs'), '4.1.2; 4.1.3.7', '62-63,68-69',
 b('Format NVM 選取已支援的 Format Index、PI type 與 metadata 傳輸方式。延伸 PI 及超過 legacy 16 個 entries 的 LBA formats 需檢查 controller ELBAS 與 host LBAFEE；沒有 host 啟用不可直接使用。', 'Format NVM selects a supported Format Index, PI type, and metadata transfer method. Extended PI and formats beyond the legacy 16 entries require controller ELBAS and host LBAFEE checks; they cannot simply be used without host enablement.'),
 b('格式切換會改變 block 數與欄位適用性，建立 buffer 前要重新 Identify。能力列表與目前格式不能混用。', 'Formatting can change block counts and field applicability; re-identify before constructing buffers. Do not confuse capability lists with the active format.'),
 [row('PI / PIL', 'PI=0 關閉，1/2/3 選 protection type', 'PI=0 disables protection; 1/2/3 select its type', '本版 PIL 必須 0，PI 位於末端', 'This revision requires PIL=0, placing PI last'), row('MSET', '1：extended LBA；0：separate metadata', 'One: extended LBA; zero: separate metadata', 'MS=0 時忽略', 'Ignored when MS=0'), row('LBAFEE', 'FID 16h byte 2，合法值 0／1', 'FID 16h byte 2, valid values zero/one', '配合 ELBAS 決定延伸格式', 'ELBAS also gates extended formats'), row('STS', 'Format 不提供自由改成非零 STS 的方法', 'Format cannot freely change STS to a new nonzero value', '新配置可由 namespace create 建立', 'Namespace creation can establish a new configuration')],
 b('要使用 64b Guard、MS=16 的格式，先確認 ELBAS、LBAFEE=1、對應 LBAF／ELBAF 及 PI capability。不能只設定 Format 的 PI=1 就宣告 64b Guard。', 'For 64b Guard with MS=16, first check ELBAS, LBAFEE=1, the corresponding LBAF/ELBAF, and PI capability. Setting Format PI=1 alone does not select 64b Guard.'),
 b('PI type 1/2/3 與 Guard width 16/32/64 是不同維度。格式變更後 NSZE／NCAP 可改變，舊 LBA Range Type hints 也可能需重設。', 'PI types 1/2/3 and Guard widths 16/32/64 are separate dimensions. NSZE/NCAP can change after formatting, and old LBA Range Type hints may need updating.'), [91,101], 'ELBAS|LBAFEE|LBAF and ELBAF|PI and metadata|Re-identify')

lesson('BASIC-FEATURES', b('基本 Features 的作用域與例外', 'Basic Feature scopes and exceptions'), '4.1.3.1-4.1.3.4', '64-67',
 b('FID 03h 描述 namespace LBA ranges，05h 控制 namespace error recovery，0Ah 控制 controller normal atomicity。Power Management 的 NVM 補充以 NPWG 大小的 Read 作為 Idle I/O Exit Latency Limit 的參考命令。', 'FID 03h describes namespace LBA ranges, 05h controls namespace error recovery, and 0Ah controls controller normal atomicity. The NVM Power Management extension uses an NPWG-sized Read as the reference for Idle I/O Exit Latency Limit.'),
 b('讀 Feature 時先辨識 scope、unit 與可儲存條件。Get、Set、Supported Capabilities 回覆也不能套用同一 payload 解碼。', 'Identify scope, units, and saveability when reading Features. Get, Set, and Supported Capabilities responses do not share one payload interpretation.'),
 [row('FID 03h', '4096-byte contiguous buffer；最多 64 個 64-byte entries', '4096-byte contiguous buffer; up to 64 64-byte entries', 'NUM 是 0-based；新 Set 取代前一次', 'NUM is zero-based; each new Set replaces prior information'), row('FID 05h.TLER', '100 ms 單位，從 error recovery 開始計時', '100 ms units, timed from error-recovery start', '0 代表不設 timeout；適用 LR 命令', 'Zero means no timeout; applies to LR-enabled commands'), row('FID 03h attributes', 'Hide／Overwriteable 是 host 使用提示', 'Hide/Overwriteable are host-use hints', '不是安全隔離或資料保護機制', 'They do not establish security isolation or data protection'), row('FID 02h extension', 'idle exit 參考 NPWG-sized Read', 'Idle-exit reference is an NPWG-sized Read', '其他命令可超過該 latency limit', 'Other commands may exceed that latency limit')],
 b('TLER=5 表示 error recovery 的 500 ms 限制，並不是從 SQ submit 起算的整筆命令 500 ms deadline。LBA Range Type 的 NUM=0 表示一個 entry。', 'TLER=5 gives an error-recovery limit of 500 ms, not a 500 ms deadline measured from SQ submission. LBA Range Type NUM=0 represents one entry.'),
 b('Figure 92 的 persistence 欄只適用於不可 save 的 Feature；可 save 時應依 saved/default 規則處理。Controller 也不必驗證 LBA Range Type 每個欄位，不能依成功 Set 推論範圍完全合理。', 'Figure 92 persistence applies only to nonsaveable Features; saveable ones follow saved/default rules. A controller need not validate every LBA Range Type field, so successful Set does not establish that all ranges are sensible.'), list(range(92,99)), 'Scope|Units|Select or Set|Persistence|Returned value')

lesson('IDENTIFY', b('Identify：同一 namespace 的多份資料結構', 'Identify: multiple structures for one namespace'), '4.1.5', '83-110',
 b('NVM CSI=00h。完整 namespace 資訊需結合 CNS 08h 的 command-set-independent 結構、CNS 00h 的 NVM 結構與 CNS 05h／CSI 00h 的 NVM 延伸結構；CNS 01h 與 06h 則提供 controller 資訊。', 'NVM uses CSI=00h. Complete namespace information combines the command-set-independent CNS 08h structure, the NVM CNS 00h structure, and NVM-specific CNS 05h/CSI 00h. CNS 01h and 06h supply controller information.'),
 b('每次查詢都寫出 CNS、CSI、NSID、Format Index 的角色。不同查詢回零有不同含義，不能一律解讀為不存在或不支援。', 'Write down the roles of CNS, CSI, NSID, and Format Index for each query. Zero-filled results have different meanings across queries and do not always mean absent or unsupported.'),
 [row('CNS 00h / 05h', 'namespace 的 NVM 基本／延伸欄位', 'Basic/extended NVM namespace fields', 'FLBAS 決定目前 FIDX；MC／DPC 是能力', 'FLBAS selects the active FIDX; MC/DPC are capabilities'), row('CNS 01h / 06h', 'AWUN 等通用位置與 NVM 專屬限制', 'Common-location AWUN fields and NVM-specific limits', '06h 的 VSL／WZSL 等需結合 variant bits', '06h VSL/WZSL fields require variant bits'), row('CNS 11h / 1Bh', 'allocated namespace 資訊', 'Allocated-namespace information', '不等同 active namespace 查詢', 'Different from active-namespace queries'), row('CNS 09h / 0Ah', '以 FIDX 查能力', 'Capability queries by FIDX', 'Common=No 欄位清零', 'Common=No fields are zeroed'), row('CNS 16h', 'namespace granularity list', 'Namespace granularity list', 'GDM 決定 descriptor 如何對應 format', 'GDM determines descriptor-to-format mapping')],
 b('NSID=7 的目前格式需讀 FLBAS 再取同 index 的 LBAF 與 ELBAF。查尚未建立的 FIDX=3 能力，使用 09h／0Ah、CSI=0、NSID=0、FIDX=3，另結合 CNS08h 的共同能力。', 'For NSID=7, read FLBAS and use that index in both LBAF and ELBAF. To query capabilities of FIDX=3 before creation, use 09h/0Ah, CSI=0, NSID=0, FIDX=3, plus common capabilities from CNS08h.'),
 b('§4.1.5.10.1 的 zoned namespace 字樣與本段 NVM 上下文不一致；教學以 CSI=00h 的 NVM namespace 解讀，並保留來源位置供查核。', 'The “zoned namespace” wording in §4.1.5.10.1 conflicts with its NVM context. This course interprets it as the NVM namespace under CSI=00h and retains the source location for review.'), [122,126,129,130,131], 'CNS and CSI|NSID or FIDX|Independent structure|NVM structures|Combine capabilities')

lesson('FORMAT-LIST', b('LBAF、ELBAF 與唯一屬性格式', 'LBAF, ELBAF, and unique-attribute formats'), '4.1.5.1; 4.1.5.3; 5.6', '85-94,96-102,160-162',
 b('LBAF 與 ELBAF 以同一 Format Index 配對；總 format 數為 raw NLBAF+1+NULBAF。NLBAF 是 0-based、NULBAF 是實際數量。有效 index 還要檢查 LBADS：值 0 表示該支援格式目前不可用。', 'LBAF and ELBAF pair at the same Format Index. Total format count is raw NLBAF+1+NULBAF: NLBAF is zero-based and NULBAF is an actual count. A valid index still requires checking LBADS; zero means that supported format is currently unavailable.'),
 b('基本 entry 給資料／metadata 大小與相對效能，extended entry 給 PI 格式與 Storage Tag 分配。Unique-attribute entries 要個別查詢，不能沿用共同能力。', 'The basic entry gives data/metadata sizes and relative performance; the extended entry gives PI format and Storage Tag allocation. Query unique-attribute entries individually instead of assuming common capabilities.'),
 [row('LBAF', 'LBADS、MS、RP', 'LBADS, MS, RP', 'RP 是指定 workload 的相對級別', 'RP is a relative class for a specified workload'), row('ELBAF', 'PIF、QPIF、STS', 'PIF, QPIF, STS', 'QPIF 只在 qualified type 下適用', 'QPIF applies only to qualified type'), row('FLBAS', 'FIDXU 與 FIDXL 組成 index', 'FIDXU and FIDXL form the index', 'MTELBA 是另一個 metadata bit', 'MTELBA is a separate metadata bit'), row('NULBAF', '追加在共同格式之後', 'Appended after common formats', '09h／0Ah 能讀各自能力', '09h/0Ah retrieve individual capabilities')],
 b('raw NLBAF=3、NULBAF=2：有 4 個共同格式及 2 個唯一屬性格式，共 6 個，合法 index 為 0..5。Figure 192 圖示採概念数量，不能直接代入未解碼的 raw NLBAF。', 'Raw NLBAF=3 and NULBAF=2 give four common formats and two unique-attribute formats: six total, indices 0..5. Figure 192 uses conceptual counts; do not substitute raw NLBAF without decoding it.'),
 b('LBADS=9 才是 512 bytes，LBADS=12 是 4096 bytes。§5.3.1.4.1 範例把 LBADS=0 寫成 512 bytes 是來源矛盾，不應用來產生命令。', 'LBADS=9 means 512 bytes and LBADS=12 means 4096 bytes. The §5.3.1.4.1 example equating LBADS=0 with 512 bytes is a source inconsistency and must not drive command construction.'), [124,125,127,128,192,193], 'NLBAF plus NULBAF|Format Index|LBAF|ELBAF|Availability')

lesson('NAMESPACE-CREATE', b('建立 namespace：格式、mask 與 granularity', 'Namespace creation: format, mask, granularity'), '4.1.6; 4.1.5.8; 5.8', '108,110-113,165',
 b('Namespace create 的 NVM payload 指定 NSZE、NCAP、FLBAS、DPS、LBSTM 與 placement handles。Namespace Size／Capacity Granularity 以 bytes 回報且是 hints；不符合 granularity 但其他條件合法時，不得僅因此拒絕 create。', 'The NVM create payload specifies NSZE, NCAP, FLBAS, DPS, LBSTM, and placement handles. Namespace Size/Capacity Granularity are byte-valued hints; a create valid in other respects must not be rejected merely for failing these granularities.'),
 b('先取得 Format Index 能力再填 host-specified fields；不可直接把整份 Identify Namespace 原封不動當作 create payload。', 'Query Format Index capabilities before filling host-specified fields; do not use an entire unmodified Identify Namespace structure as the create payload.'),
 [row('NSZE / NCAP', '值以 logical blocks 指定', 'Values are specified in logical blocks', '與 bytes granularity 比較前先換算', 'Convert before comparing with byte granularities'), row('LBSTM', '需符合 PIC／PIFA mask 約束', 'Must satisfy PIC/PIFA masking constraints', '不符回 Invalid Field in Command', 'Violations return Invalid Field in Command'), row('GDM / ND', 'GDM=0 使用 descriptor 0 對全部格式', 'GDM=0 uses descriptor zero for all formats', 'ND 是 0-based', 'ND is zero-based'), row('Completion', '成功 create 後已按指定屬性 format', 'Successful creation includes the requested formatting', 'attachment 是另一個管理動作', 'Attachment is a separate management action')],
 b('假設 NSG=1 MiB、NCG=1 MiB、logical block size=4096，NSZE=NCAP=256 的容量可完整定址；若改成 257，granularity hints 可能造成額外不可定址配置，但本身不是拒絕理由。', 'With NSG=NCG=1 MiB and logical block size 4096, NSZE=NCAP=256 makes the allocation fully addressable. Choosing 257 may leave extra unaddressable allocation under the hints, but that alone is not a rejection reason.'),
 b('Masking Not Supported 代表 Storage Tag mask 有效 bits 全為 1；不是全零。不支援或未啟用 FDP 時，placement 欄位也不能套用啟用後的語意。', 'Masking Not Supported requires all significant Storage Tag mask bits to be one, not zero. Placement fields do not acquire enabled-FDP semantics when FDP is unsupported or disabled.'), [132,133,134], 'Format capability|NSZE and NCAP|PI and LBSTM|Placement handles|Create result')

lesson('FDP', b('FDP：placement、RUH 與可觀測數據', 'FDP: placement, RUHs, and observable data'), '3.2.1; 4.1.4.6-4.1.4.7; 4.1.6.3', '26,79,110-113',
 b('FDP namespace 的 placement handles 對應 Reclaim Unit Handles。共享 RUH 的 namespaces 必須使用相同 Format Index；host 指定的 handle list 不得重複 RUHID。RUH Status descriptors 先依 Placement Handle、再依 Reclaim Group Identifier 排序。', 'FDP namespace placement handles map to Reclaim Unit Handles. Namespaces sharing a RUH must use the same Format Index, and host-specified handle lists cannot repeat RUHIDs. RUH Status descriptors sort first by Placement Handle, then by Reclaim Group Identifier.'),
 b('建立時決定 placement 關係，執行時看 handle status，事後再用 statistics／events 解釋媒體搬移。三種資料不能互相代替。', 'Creation establishes placement relationships; handle status describes operation, and statistics/events explain later media movement. These evidence types are not interchangeable.'),
 [row('NPHNDLS', '最多 128，且不超過 configuration 的 RUH 數', 'At most 128 and no more than the configuration RUH count', '0 有 controller 選擇與共享規則', 'Zero invokes controller-selection and sharing rules'), row('EARUTR / RUAMW', '剩餘秒數估計／可寫 logical blocks', 'Estimated remaining seconds / writable logical blocks', 'EARUTR=0 未回報；不是保證壽命', 'EARUTR=0 means unreported; not a lifetime guarantee'), row('LID 22h', 'HBMW／MBMW 含 NVM 指定寫入類命令', 'HBMW/MBMW include the specified NVM write-class commands', '含 Copy 寫入端、Zeroes、Uncorrectable', 'Includes Copy destination, Zeroes, and Uncorrectable'), row('LID 23h event 0h', 'LBAV 控制 LBA 有效性', 'LBAV governs LBA validity', 'NLBAM=FFFFh 表示至少 FFFFh', 'NLBAM=FFFFh means at least FFFFh')],
 b('兩個 namespaces 共享 RUH 5，第一個使用 FIDX=2，第二個 create 指定 FIDX=3，即使資料大小同為 4 KiB，也不符合共享 RUH 的 Format Index 條件。', 'If two namespaces share RUH 5 and one uses FIDX=2, creating the other with FIDX=3 violates the shared-RUH Format Index rule even if both have 4 KiB data blocks.'),
 b('Media Reallocated event 的 LBA 是搬移集合中的一個 LBA，並不是完整清單；LBAV=0 時必須忽略。', 'The LBA in a Media Reallocated event is one member of the moved set, not a complete list; ignore it when LBAV=0.'), [21,116], 'Create placement|PID and RUHID|Handle status|Statistics|Media event')

lesson('LOG-EVENTS', b('AER、SMART 與錯誤記錄的 NVM 補充', 'NVM extensions to AER, SMART, and error records'), '1.4.2; 4.1.1; 4.1.3.5; 4.1.4.1-4.1.4.4', '10-11,62,67,75-77',
 b('NUSE 的頻繁改變及 ANA 造成的 capacity 回報變動不產生 Namespace Attribute Changed 事件。Error Information 的 LBA 指最低發生錯誤的 LBA；Self-test FLBA 只在 valid bit 設定時有效，且可只代表多個失敗 blocks 中的一個。', 'Frequent NUSE changes and ANA-induced capacity-report changes do not generate Namespace Attribute Changed events. Error Information reports the lowest erroneous LBA. Self-test FLBA is meaningful only with its valid bit and may identify only one of several failing blocks.'),
 b('用事件找調查方向，再用正確 scope 的 log 與 command set 定義解碼。統計數據也要依命令分類，不能只按 opcode 名稱猜。', 'Use events to direct investigation, then decode logs using their scope and command-set definitions. Classify commands correctly when interpreting statistics instead of guessing from opcode names.'),
 [row('LBASIN / RLCCN', 'FID0Bh bits 13／22', 'FID0Bh bits 13/22', '分別啟用 LBA Status／Rate Limiting notices', 'Enable LBA Status / Rate Limiting notices'), row('SMART units', '先換算 512-byte units，再套 Base counter 編碼', 'Convert to 512-byte units, then apply Base counter encoding', '不是每個 4 KiB block 加一個 Data Unit', 'One 4 KiB block is not one Data Unit'), row('Read categories', 'Data Units Read 含 Verify；Host Read 含 Copy', 'Data Units Read includes Verify; Host Read includes Copy', 'Compare、Read 為兩類共同項', 'Compare and Read belong to both categories'), row('Persistent Event 06h', 'create／single-delete 有 FLBAS、DPS', 'Create/single-delete records contain FLBAS and DPS', 'delete-all 時這兩欄 reserved', 'Both fields are reserved for delete-all')],
 b('一個 Self-test FLBA=100、valid=1 的紀錄只能定位一個失敗 LBA，不能據此宣告其他 LBAs 全部正常。Error Information LBA=100 的「最低」語意也不能直接套到 FLBA。', 'A Self-test record with FLBA=100 and valid=1 identifies one failing LBA, not a clean bill of health for all others. Error Information’s “lowest LBA” semantics cannot be applied to FLBA.'),
 b('Rate Limiting log 使用 CSI，其他表列 logs 多不使用；不要把相同 LID 的不同 command-set 定義混合。', 'The Rate Limiting log uses CSI, while most other listed logs do not; do not mix command-set definitions for the same LID.'), [90,99,109,110,111,112], 'Notice enable|AER|Log scope|Validity and units|Correlate evidence')

lesson('LBA-STATUS', b('LBA Status：通知、掃描與修復流程', 'LBA Status: notices, scans, and recovery'), '4.1.3.6; 4.1.4.5; 4.2.1; 5.2.1', '67-68,77-79,114-122',
 b('LID 0Eh 先指出值得調查的 namespace ranges；Get LBA Status 再回詳細 descriptors。ATYPE=02h 回 tracked allocated LBAs，10h 掃描並回 tracked／untracked 候選，11h 只回 tracked 候選且不做 foreground scan。', 'LID 0Eh first identifies namespace ranges worth investigating; Get LBA Status returns detailed descriptors. ATYPE=02h reports tracked allocated LBAs, 10h scans and returns tracked/untracked candidates, and 11h returns tracked candidates without a foreground scan.'),
 b('讀到 potentially unrecoverable 不是每個 block 一定無法讀取。先辨識 ATYPE，再檢查 buffer 是否足夠與 completion condition，最後安排資料修復。', 'Potentially unrecoverable does not mean every block necessarily fails to read. Identify ATYPE, check buffer capacity and completion condition, then arrange data recovery.'),
 [row('MNDW / RL', 'MNDW 是 0-based dwords；RL=0 到 NSZE−1', 'MNDW is zero-based dwords; RL=0 runs through NSZE−1', '不是 RL=0 查一個 block', 'RL=0 does not query one block'), row('NLSD / CMPC', '實際 descriptor 數／完成原因', 'Actual descriptor count / completion reason', 'CMPC=1 尚有資料或 scan 未完成；2 完成', 'CMPC=1 means more data or incomplete scan; 2 means complete'), row('LSIPI / LSIRI', '100 ms 單位；poll interval 不可由 host 改', '100 ms units; host cannot change the poll interval', 'Set 回傳最接近支援值', 'Set returns the closest supported value'), row('RAE / LSGC', 'RAE=1 分段讀，RAE=0 清事件並允許更新', 'Read chunks with RAE=1; RAE=0 clears the event and allows updates', '重讀 header 檢查 generation', 'Reread the header and check generation'), row('TLBAAG', '02h 可用較大 allocation granularity', '02h may report allocation at a larger granularity', '混合 allocated／deallocated unit 會整段回 allocated', 'A mixed unit is reported allocated as a whole')],
 b('log 有 NLSLNE=0 但 ESTULB 非零時，不能當作沒有問題，宜檢查 attached namespaces 的完整 LBA 範圍。取得可疑 LBAs 後，可從其他可靠來源恢復並寫回；後續查詢會移除成功重寫且未再偵測的項目。', 'NLSLNE=0 with nonzero ESTULB is not an all-clear: examine the full ranges of attached namespaces. Recover suspect LBAs from another reliable source and rewrite them; later queries remove successfully rewritten entries unless newly detected again.'),
 b('CMPC=1 的 command success 不是全範圍掃描完成。Log 的 RNLB 與詳細 descriptor 的 NLB 都是 0-based，NLSD 卻是實際筆數。', 'Command success with CMPC=1 does not mean the whole range was scanned. Log RNLB and detailed-descriptor NLB are zero-based, while NLSD is an actual count.'), [100,113,114,115]+list(range(135,141))+[142,143,144], 'LID 0Eh|ATYPE|Range and MNDW|CMPC and descriptors|Recover and recheck')

lesson('PERFORMANCE-FEATURE', b('Performance Characteristics 的屬性模型', 'Performance Characteristics attributes'), '4.1.3.8', '69-73',
 b('FID 1Ch 用 ATTRI 選屬性：00h 為只讀標準效能，C0h 為只讀 identifier list，C1h..FFh 為 vendor attributes。Current、Default、Saved 是不同視圖，RVSPA 用來移除指定 vendor attribute 的 saved value。', 'FID 1Ch selects attributes through ATTRI: 00h is read-only standard performance, C0h a read-only identifier list, and C1h..FFh vendor attributes. Current, Default, and Saved are distinct views; RVSPA removes a selected vendor attribute’s saved value.'),
 b('此 Feature 回報或管理效能屬性，不能把標準 latency 級別當成對任意 workload 的服務保證。', 'This Feature reports or manages performance attributes; its standard latency class is not a service guarantee for arbitrary workloads.'),
 [row('R4KARL', '標準化 4 KiB random read 的平均 latency 區間', 'Average-latency range for standardized 4 KiB random reads', '00h 是未回報', '00h means unreported'), row('MSVSPA / USVSPA', '可 save 總數／剩餘數', 'Saveable capacity / unused capacity', 'index 可不連續', 'Indices can be noncontiguous'), row('PAID / ATTRL', '128-bit identifier／有效 vendor bytes', '128-bit identifier / valid vendor bytes', 'ATTRL 最大 FE0h', 'ATTRL is at most FE0h'), row('RVSPA', '刪除 saved value 後取 default', 'Delete the saved value to return to default', '此操作不使用 data buffer 內容', 'The data-buffer contents are unused')],
 b('R4KARL=0Eh 表示 50 μs ≤ 平均 latency <100 μs，並非 14 μs。讀 C0h list 時應使 ATTRTYP 與 Get 的 SEL 一致，再用 PAID 解釋 vendor payload。', 'R4KARL=0Eh means 50 μs ≤ average latency <100 μs, not 14 μs. When reading the C0h list, match ATTRTYP with Get SEL and use PAID to interpret vendor payloads.'),
 b('Set ATTRI=00h 或 C0h 會回 Invalid Field in Command。Vendor bytes 的意義需要廠商定義，不能自行推測成標準欄位。', 'Set with ATTRI=00h or C0h returns Invalid Field in Command. Vendor-byte meanings require a vendor definition and cannot be invented as standard fields.'), list(range(102,106)), 'Scope|ATTRI|Current default saved|PAID and length|Interpretation')

lesson('RATE-CONFIG', b('Rate Limiting 的設定欄位', 'Rate Limiting configuration fields'), '4.1.3.9; 4.1.5.4; 5.10', '73-75,106,165-168',
 b('FID 28h 以 TGT／TID 選 target，1024-byte buffer 指定 enable、mode、bandwidth、IOPS 與 write/read ratios。TGT=0 指 controller，不能指定 Admin controller 或保留 ID。此 Feature 必須可 save。', 'FID 28h selects a target with TGT/TID and uses a 1024-byte buffer for enablement, mode, bandwidth, IOPS, and write/read ratios. TGT=0 selects a controller; an Admin controller or reserved ID is invalid. This Feature must be saveable.'),
 b('先從 LID 28h 取得支援 target，再檢查 HLS／SLS 與 soft-controller 數量。把 host 請求限制和裝置可達能力分開記錄。', 'Obtain supported targets from LID 28h, then check HLS/SLS and the soft-controller count. Record requested limits separately from attainable device capabilities.'),
 [row('TGT / TID', 'CDW11[23:16]／[15:0]', 'CDW11[23:16] / [15:0]', 'TGT=0 才是標準 controller target', 'TGT=0 is the standard controller target'), row('RLC', 'RLE bit15；RLM=0 Hard、1 Soft', 'RLE bit15; RLM=0 Hard, 1 Soft', '支援 Soft 必須也支援 Hard', 'Soft support requires Hard support'), row('BWSF', '0/1/2 = 1/10/100 MiB/s；3/4/5 = 1/10/100 GiB/s', '0/1/2 = 1/10/100 MiB/s; 3/4/5 = 1/10/100 GiB/s', '值乘 scale 才是 bandwidth', 'Multiply the value by the scale'), row('WRIOPSR / WRBWR', 'write 分子除 read 分母', 'Write numerator divided by read denominator', '兩者的各 ratio bytes 均需非零', 'Every ratio component byte must be nonzero')],
 b('BWSF=1、TBWV=50 表示 500 MiB/s。WBWV 與 WIOPS 控制寫入部分，總量還會按 WRBWR／WRIOPSR 加權；不能把 total 限制只當 read limit。', 'BWSF=1 and TBWV=50 specify 500 MiB/s. WBWV/WIOPS govern writes, while total consumption is weighted by WRBWR/WRIOPSR; total limits are not simply read limits.'),
 b('未替某 target 設 limits 時，其 limits 是 vendor-specific；不能把缺少設定解讀為無限制。§5.10 開頭的 Hard／Soft 小節引用對調，正確是 5.10.1 Hard、5.10.2 Soft。', 'Limits for an unconfigured target are vendor-specific, not necessarily unlimited. The opening §5.10 references interchange Hard/Soft sections: 5.10.1 is Hard and 5.10.2 is Soft.'), [106,107,108], 'Supported targets|HLS and SLS|TGT and TID|Limits and ratios|Set and Get')

lesson('RATE-GRAPH', b('Rate Limiting log 是能力圖', 'The Rate Limiting log is a capability graph'), '4.1.4.8; 5.10.3', '79-83,168-172',
 b('LID 28h 以 log 起點為基準的 dword offsets 連接 port、controller 與 storage-medium access descriptors；descriptor 可共享，不能假設為固定順序陣列。分段讀取後應重讀 GC，改變時重收整份資料。', 'LID 28h links ports, controllers, and storage-medium access descriptors using dword offsets from the log start. Descriptors may be shared rather than forming one fixed-order array. After chunked reads, reread GC and recollect if it changed.'),
 b('先做結構邊界檢查，再分析 bandwidth bottleneck。Port 的能力與共同 Endurance Group 的能力不同，不能把所有數字直接相加。', 'Validate structure bounds before analyzing bandwidth bottlenecks. Port capability differs from shared Endurance Group capability; do not simply add all reported numbers.'),
 [row('LPL / offsets', '長度與指標皆有 dword 單位', 'Lengths and pointers use dword units', 'byte offset = dword offset ×4', 'Byte offset = dword offset ×4'), row('NP / NC / NST', '都是 0-based counts', 'All are zero-based counts', 'NNSMAD 是實際數量', 'NNSMAD is an actual count'), row('SC / SI', 'subsystem／domain／EG／namespace 及其 ID', 'Subsystem/domain/EG/namespace and its ID', '依 scope 解讀，避免共享節點重算', 'Interpret scope and avoid double-counting shared nodes'), row('RLMA', '最大 read/write bandwidth／IOPS', 'Maximum read/write bandwidth/IOPS', 'workload 需符合相關 size／queue-depth 條件', 'Relevant size/queue-depth workload conditions apply')],
 b('兩個 PCIe ports 都能使用一個 Endurance Group，若 EG 的 bandwidth 已被 controller 0 用滿，controller 1 不會因多一個 port 就多一份媒體頻寬。相同 EG 能力可由兩個 controller 指向同一 descriptor。', 'Two PCIe ports may share one Endurance Group. If controller 0 consumes all EG bandwidth, the extra port does not create more media bandwidth for controller 1. Both controllers may reference one descriptor for identical EG access capabilities.'),
 b('Figures 196／198 的範例 offsets、byte ranges 與 LPL 有內部矛盾。本報告重畫關係並用 offset×4、descriptor length、LPL bounds 校驗，不把原範例當可直接執行的 binary fixture。', 'Examples in Figures 196/198 contain inconsistent offsets, byte ranges, and LPL. This course redraws the relationships and checks offset×4, descriptor length, and LPL bounds instead of treating the examples as executable binary fixtures.'), list(range(117,122))+[195,196,197,198], 'Header and GC|Port offsets|Controller offsets|Shared storage nodes|Bounds and bottleneck')

lesson('RATE-MODES', b('Hard／Soft 與 token-bucket 算例', 'Hard/Soft modes and token-bucket examples'), '5.10.1-5.10.2; Appendix A', '166-168,176-177',
 b('Hard Limit 設 ceiling，Soft Limit 可利用未用 bandwidth／IOPS；資源不足時依設定比例分配。Appendix A 是多個 token buckets 的實作範例，不要求所有 controllers 採用相同內部實作。', 'Hard Limit sets a ceiling; Soft Limit can use unused bandwidth/IOPS. Resource shortfalls are shared proportionally to configured limits. Appendix A illustrates multiple token buckets without requiring one identical controller implementation.'),
 b('用能力、limits、實際 demand 三個值判讀結果。設定比例不等於任何時刻都固定吞吐；內部資源與工作負載仍會改變觀測值。', 'Interpret results using capability, configured limits, and actual demand. A configured ratio does not force a fixed throughput ratio at every instant; internal resources and workload still matter.'),
 [row('Hard', '有需求且資源不足時按比例分享', 'Proportional sharing under demand and resource shortage', '設定上限不是最低效能保證', 'A configured ceiling is not a performance floor'), row('Soft', '可使用閒置額度', 'May consume unused capacity', '多個 soft targets 依 limits 比例分享', 'Multiple soft targets share it in proportion to limits'), row('Write tokens', 'total bytes × WRBWR；write bytes；total IOPS × WRIOPSR；write IOPS 1', 'Total bytes × WRBWR; write bytes; total IOPS × WRIOPSR; one write IOPS', '四個 buckets 各自檢查', 'Check each of four buckets'), row('Read tokens', 'total bytes 及 total IOPS 1', 'Total bytes and one total IOPS', '不扣 write-only buckets', 'Does not consume write-only buckets')],
 b('教學設定：4 KiB Write，WRBWR=2、WRIOPSR=3，分別消耗 total-bandwidth 8 KiB、write-bandwidth 4 KiB、total-IOPS 3、write-IOPS 1。4 KiB Read 只消耗 total-bandwidth 4 KiB 與 total-IOPS 1。', 'Teaching setup: a 4 KiB Write with WRBWR=2 and WRIOPSR=3 consumes 8 KiB total-bandwidth, 4 KiB write-bandwidth, three total-IOPS, and one write-IOPS token. A 4 KiB Read consumes only 4 KiB total-bandwidth and one total-IOPS token.'),
 b('Token 不足時延後處理而非丟棄命令；可以處理部分，但不得在整筆處理完前先送 completion。', 'Insufficient tokens defer commands rather than drop them. Partial processing is allowed, but completion cannot precede processing of the entire command.'), [202], 'Capabilities|Configured limits|Actual demand|Token admission|Completion')

lesson('ALIGNMENT', b('對齊、granularity 與效能提示', 'Alignment, granularity, and performance hints'), '5.2.2', '122-129',
 b('NPWG／NPWA、NPRG／NPRA 分別描述寫入與讀取的建議大小及對齊，NOWS／NORS 描述最佳大小。這些 performance hints 不取代 atomic boundaries、命令硬限制或格式規則。', 'NPWG/NPWA and NPRG/NPRA describe recommended write and read granularities and alignments; NOWS/NORS describe optimal sizes. These performance hints do not replace atomic boundaries, command hard limits, or format rules.'),
 b('先把 raw 欄位轉成 blocks，再評估 start alignment 與 length granularity。只滿足其中一項可能仍引發 read-modify-write。', 'Decode raw fields into blocks, then evaluate starting alignment and length granularity. Meeting only one can still cause read-modify-write.'),
 [row('NPWG / NPWA', '長度與起點同時符合', 'Meet both length and start recommendations', '先看 NSFEAT.OPTPERF', 'First check NSFEAT.OPTPERF'), row('NPRG / NPRA / NORS', '適用讀取最佳化', 'Guide read optimization', '先看 OPTRPERF', 'First check OPTRPERF'), row('NPDG / NPDGL', 'deallocate granularity 的不同欄位', 'Alternative deallocate-granularity fields', '用 OPTPERF 決定；Large 不一律加一', 'Select via OPTPERF; Large fields do not universally add one'), row('NOIOB / NABO', '最佳 I/O boundary 與 atomic offset 不同', 'Optimal I/O boundary differs from atomic offset', '可分割 I/O 以符合多種條件', 'I/O can be split to satisfy multiple conditions')],
 b('解碼後 NPWG=8、NPWA=8。寫 LBA 8..15 同時符合；寫 9..16 雖然也是 8 blocks，仍跨兩個 granularity units，可能需要讀取兩端舊資料。', 'With decoded NPWG=8 and NPWA=8, writing LBAs 8..15 meets both. Writing 9..16 also has eight blocks but crosses two units and may require reading old data at both ends.'),
 b('來源圖的 read-modify-write 是可能機制，不是每台 SSD 一定執行的內部流程。Namespace 重新 format 後，舊的 alignment hints 可能失效。', 'The source’s read-modify-write diagrams illustrate a possible mechanism, not a mandatory internal implementation on every SSD. Reformatting can invalidate old alignment hints.'), list(range(145,152)), 'Support bits|Decode units|Align start|Choose length|Measure workload')

lesson('METADATA', b('Metadata 傳輸與 PI 的位置', 'Metadata transfer and PI placement'), '2.1.6; 5.2.3; 5.3', '22,129-131',
 b('每個 namespace 在 format 時選一種 metadata 傳輸機制：與 data 相連形成 extended LBA，或由 MPTR 指向 separate buffer。不能把 metadata 分拆到兩種機制；寫入時 metadata 必須與其 logical block 原子寫入。', 'Formatting selects one metadata transfer mechanism per namespace: contiguous extended LBAs or a separate buffer addressed by MPTR. Metadata cannot be split between the mechanisms and must be written atomically with its associated logical block.'),
 b('Metadata 不一定全是 PI。先標示 data、非 PI metadata 與 PI 三個區域，再計算 host buffer 大小與 CRC coverage。', 'Metadata need not consist entirely of PI. Mark data, non-PI metadata, and PI separately before computing host-buffer size and CRC coverage.'),
 [row('Extended LBA', 'DPTR 指向 data+metadata 交錯序列', 'DPTR addresses interleaved data+metadata', 'MSET／MTELBA 反映此選擇', 'MSET/MTELBA reflect the selection'), row('Separate buffer', 'DPTR 給 data，MPTR 給 metadata', 'DPTR addresses data; MPTR addresses metadata', 'PRP metadata 需 physically contiguous；SGL 可分散', 'PRP metadata is physically contiguous; SGL metadata may be scattered'), row('PI location', '本版有效格式的 PI 在 metadata 末端', 'Valid current formats put PI at the metadata end', 'CRC 包含之前的非 PI metadata', 'CRC covers preceding non-PI metadata')],
 b('8 blocks、data=4096、MS=16、PRACT=0：extended buffer 為 32896 bytes；separate 模式 data buffer=32768、metadata buffer=128 bytes。', 'For eight blocks, data=4096, MS=16, PRACT=0: the extended buffer is 32896 bytes; separate buffers are 32768 data bytes and 128 metadata bytes.'),
 b('PI 在末端的位置規則與「metadata 是否 separate」是獨立維度；不能以 DIX／DIF 名稱省略 namespace format 設定。', 'PI placement at the end and separate-versus-extended metadata are independent choices; DIF/DIX terminology does not replace namespace-format settings.'), [153,154], 'Namespace format|Data region|Metadata region|PI suffix|Buffer lengths')

lesson('PI-FORMATS', b('16／32／64b Guard 與 Qualified PI', '16/32/64b Guard and qualified PI'), '5.3.1; 4.1.5.3', '97-102,130-138',
 b('16b Guard PI 共 8 bytes；32b 與 64b Guard PI 各 16 bytes。Application Tag 固定 16 bits，Storage/Reference Space 分別為 32、80、48 bits。32b／64b 格式限 logical block data size 至少 4 KiB。', '16b Guard PI occupies eight bytes; 32b and 64b Guard PI each occupy 16 bytes. Application Tag is 16 bits, and Storage/Reference Space is respectively 32, 80, or 48 bits. The 32b/64b formats require logical block data size of at least 4 KiB.'),
 b('先由 PIF 決定格式；PIF=11b 才由 QPIF 決定 Guard width，並受 QPIFS 與 STMLA 條件限制。Protection type 仍由 DPS 決定。', 'Select the format with PIF; only PIF=11b delegates Guard width to QPIF, subject to QPIFS and STMLA. DPS separately selects protection type.'),
 [row('16b Guard', '2-byte Guard + 2-byte App + 4-byte space', '2-byte Guard + 2-byte App + 4-byte space', 'STS=0..32', 'STS=0..32'), row('32b Guard', '4-byte Guard + 2-byte App + 10-byte space', '4-byte Guard + 2-byte App + 10-byte space', 'STS=16..64', 'STS=16..64'), row('64b Guard', '8-byte Guard + 2-byte App + 6-byte space', '8-byte Guard + 2-byte App + 6-byte space', 'STS=0..48', 'STS=0..48'), row('STMLA', 'bit mask／byte mask／no mask', 'Bit masking / byte masking / no masking', 'qualified type 與 QPIFS 共同決定適用', 'Qualified type and QPIFS determine applicability')],
 b('64b Guard、STS=18：48-bit space 中高 18 bits 是 Storage Tag、低 30 bits 是 Reference Tag。PI 總大小仍為 16 bytes，不會因 STS 增加而變大。', 'With 64b Guard and STS=18, the high 18 bits of the 48-bit space form Storage Tag and the low 30 bits form Reference Tag. Total PI remains 16 bytes; increasing STS does not enlarge it.'),
 b('本版保留 qualified type，不能把 PIF=11b 一概當 reserved。STS 等於整個 space 時 Reference Tag 不存在；STS=0 時 Storage Tag 不存在。', 'This revision defines qualified type, so PIF=11b is not universally reserved. Reference Tag is absent when STS consumes the entire space; Storage Tag is absent when STS=0.'), [155,156,157,159,164,165], 'DPS type|PIF or QPIF|Guard size|STS split|Mask capability')

lesson('CRC', b('CRC 參數、位元順序與已知向量', 'CRC parameters, bit order, and known vectors'), '5.3.1.1-5.3.1.3', '131-137',
 b('32b Guard 使用 CRC-32C；64b Guard 使用 NVM Express 64b CRC，polynomial AD93D23594C93659h、全一 Init／XorOut、RefIn／RefOut=true。不能只用「CRC64」名稱選任意 polynomial。', '32b Guard uses CRC-32C. 64b Guard uses the NVM Express 64b CRC with polynomial AD93D23594C93659h, all-ones Init/XorOut, and RefIn/RefOut=true. The label CRC64 alone does not select the correct polynomial.'),
 b('CRC 的 polynomial、初值、reflection、final XOR 與儲存順序必須一起核對。用已知向量驗證後才接進 PI parser。', 'Check polynomial, initialization, reflection, final XOR, and storage order together. Validate known vectors before integrating a PI parser.'),
 [row('CRC-16', 'SBC-4 定義的 Guard CRC', 'Guard CRC defined by SBC-4', 'NVM 不支援 DIX 的 optional IP checksum', 'NVM does not support the optional DIX IP checksum'), row('CRC-32C', 'polynomial 1EDC6F41h', 'Polynomial 1EDC6F41h', '4 KiB zero vector → 98F94189h', '4 KiB zero vector → 98F94189h'), row('CRC-64/NVME', '反射式 register 算例 123456789 → AE8B14860A799888h', 'Reflected-register example 123456789 → AE8B14860A799888h', '4 KiB zero vector → 6482D367EB22B64Eh', '4 KiB zero vector → 6482D367EB22B64Eh'), row('Coverage', 'data + PI 前的 metadata', 'Data plus metadata preceding PI', '排除 PI 本身', 'Exclude PI itself')],
 b('CRC-64 的 4 KiB 全 FFh 向量結果為 C0DDBA7302ECA3ACh。若 zero vector 正確而 incrementing-byte vector 不符，要檢查 byte／bit 順序，不能只改 polynomial 硬湊。', 'The CRC-64 result for 4 KiB of FFh is C0DDBA7302ECA3ACh. If the zero vector passes but the incrementing-byte vector fails, inspect byte/bit order rather than changing the polynomial to fit.'),
 b('Figure 161 列 Check=11199E506128D175h；常見 LSB-first register 對 123456789 算得 AE8B14860A799888h，兩者互為 64-bit 反轉。Figure 163 的四個 4 KiB 向量則直接符合該 register 結果。須明示表示差異，不因單一 Check 差異改 polynomial；CRC 也不是密碼學認證。', 'Figure 161 lists Check=11199E506128D175h; a conventional LSB-first register computes AE8B14860A799888h for 123456789. They are 64-bit reversals. All four Figure 163 4 KiB vectors directly match that register result. State this representation discrepancy rather than changing the polynomial to fit one Check value. CRC is not cryptographic authentication.'), [158,160,161,162,163], 'Data and metadata|Initialization|Polynomial and reflection|Final XOR|Known vectors')

lesson('TAG-LAYOUT', b('Storage／Reference Tag 的 Dword 封裝', 'Packing Storage/Reference Tags into Dwords'), '5.3.1.4', '137-141',
 b('Storage Tag 使用 Storage/Reference Space 的高 STS bits，其餘低 bits 是 Reference Tag。命令以 CDW2、CDW3、CDW14 的最多 80 bits 傳入實際或 expected tags；不同 Guard 格式使用不同子集合。', 'Storage Tag occupies the high STS bits of Storage/Reference Space, with Reference Tag in the remaining low bits. Commands supply actual or expected tags in up to 80 bits across CDW2, CDW3, and CDW14; Guard formats use different subsets.'),
 b('先決定總 space 大小，再切 tag，最後拆到命令 Dwords。不要因 tag 的名稱相似就把 Write 的值與 Read 的 expected 值交換。', 'Determine total space width, split the tags, then pack command Dwords. Similar names do not make Write values interchangeable with Read expected values.'),
 [row('16b Guard, STS=0', 'CDW14 為 32-bit reference', 'CDW14 contains the 32-bit reference', 'CDW2／3 對此 tag 忽略', 'CDW2/3 are ignored for this tag'), row('32b Guard, STS=32', 'CDW2 low16 + CDW3 high16 是 Storage', 'CDW2 low16 + CDW3 high16 carry Storage', 'CDW3 low16 + CDW14 為 48-bit Reference', 'CDW3 low16 + CDW14 carry the 48-bit Reference'), row('64b Guard, STS=18', 'CDW3 low16 + CDW14 high2 是 Storage', 'CDW3 low16 + CDW14 high2 carry Storage', 'CDW14 low30 是 Reference', 'CDW14 low30 carry Reference')],
 b('64b Guard、STS=18、Storage Tag=0x12345、Reference Tag=0x2A：CDW3 low16=0x48D1，CDW14=(1<<30)|0x2A=0x4000002A。Read 使用相同布局的 expected tags。', 'For 64b Guard, STS=18, Storage Tag=0x12345, Reference Tag=0x2A: CDW3 low16=0x48D1 and CDW14=(1<<30)|0x2A=0x4000002A. Read uses expected tags in the same layout.'),
 b('Figure 171 對 CDW3 的 Storage Tag bit range 與下一列重疊；以 Figure 166 與對稱的 Figure 170 判讀為 high16，並記錄這是來源矛盾的工程解讀。', 'Figure 171 gives a CDW3 Storage Tag range overlapping the next row. Figures 166 and 170 support interpreting it as high16; this is an explicitly identified engineering interpretation of a source inconsistency.'), list(range(166,174)), 'PI space width|STS|Storage and reference|CDW2 and CDW3|CDW14')

lesson('PI-CHECKING', b('PRACT 與 PRCHK／STC 的組合', 'Combining PRACT with PRCHK/STC'), '2.1.5; 5.3.2-5.3.3', '21-22,141-152',
 b('PRACT 決定 PI 的傳遞、插入、移除或取代；PRCHK 的 Guard／Application／Reference bits 與獨立 STC 決定檢查要求。PRACT=1 且 MS>PI size 時，Read 仍傳回全部 metadata，不能一律解讀為刪除 PI。', 'PRACT controls PI transfer, insertion, stripping, or replacement. PRCHK Guard/Application/Reference bits and independent STC control checking. With PRACT=1 and MS>PI size, Read still returns all metadata; PRACT=1 does not universally mean stripping PI.'),
 b('先檢查 namespace 是否啟用 PI，再依命令方向及 metadata 大小選處理分支。Checking bits 與可能的特殊停用值放在最後判斷。', 'First determine whether namespace PI is enabled, then choose a processing branch by command direction and metadata size. Evaluate checking bits and special disable values afterward.'),
 [row('Write, PRACT=1', 'MS=PI 時插入；MS>PI 時取代 PI', 'Insert PI when MS=PI; replace it when MS>PI', '此生成分支忽略 PRCHK／STC', 'This generation branch ignores PRCHK/STC'), row('Read, PRACT=1', '先做要求的檢查；MS=PI 才移除', 'Perform requested checks; strip only when MS=PI', 'MS>PI 仍回 metadata 與 PI', 'MS>PI still returns metadata including PI'), row('Type 1 / Type 2', 'Reference 每個 block 遞增', 'Reference increments per block', 'Type1 初值須等於對應 SLBA 低 bits', 'Type1 initial value must match relevant low SLBA bits'), row('Type 3', '不宜比對 computed reference', 'Should not compare a computed reference', '若因 RTCHK 拒絕，使用 Invalid Protection Information', 'An RTCHK rejection uses Invalid Protection Information'), row('Disable sentinels', 'Type 1／2：Application Tag=FFFFh 時停用所有 PI checks；Type 3 另要求 Reference Tag（若有）也全一', 'Type 1/2 disable all PI checks when Application Tag=FFFFh; Type 3 additionally requires an all-ones Reference Tag, if defined', '不受 PRCHK／STC 設定影響', 'Overrides PRCHK/STC settings'), row('Masks', 'mask bit=0 不比較', 'A zero mask bit excludes comparison', 'Storage mask 另受 STMLA 約束', 'Storage masking also obeys STMLA')],
 b('16b Guard、MS=16、Read PRACT=1：host 仍接收 16 bytes metadata；若 MS=8，則 host 只接收 data。相同 PRACT 在不同 MS 下造成不同 buffer 大小。', 'For 16b Guard, MS=16, Read PRACT=1, the host still receives 16 metadata bytes. With MS=8 it receives data only. The same PRACT value produces different buffer lengths under different MS.'),
 b('STC 在本份指 Storage Tag Check；不要沿用 Self-test Code 的縮寫解釋。Compare 的兩條輸入路徑都可能執行 PI checking，但 PI bytes 不納入一般 metadata 比對。', 'STC here means Storage Tag Check, not Self-test Code. Both Compare input paths can perform PI checking, while PI bytes are excluded from ordinary metadata comparison.'), [11,12,174,175,176], 'PI enabled|PRACT|Metadata size|PRCHK and STC|Status')

lesson('ANA-RESERVATIONS', b('ANA 與 Reservations 的 NVM 行為', 'NVM behavior under ANA and Reservations'), '5.1; 5.11', '119,172-173',
 b('ANA 狀態會限制指定 Features 並改變 capacity 回報；Reservations 則依 reservation type、holder 與 registration 狀態決定各命令是否允許。這些共用 namespace 能力可適用於 PCIe 多 controller 情境。', 'ANA state can restrict specified Features and alter capacity reporting. Reservations determine command permissions from reservation type, holder, and registration state. These shared-namespace capabilities can apply in PCIe multi-controller configurations.'),
 b('同一 namespace 的可達性與存取權限要分開檢查；不能將路徑狀態等同 reservation ownership。', 'Check namespace access state separately from access permissions; path state does not establish reservation ownership.'),
 [row('ANA Identify', 'Inaccessible／Persistent Loss 下 NUSE、NVMCAP 回零', 'NUSE/NVMCAP return zero in Inaccessible/Persistent Loss', '不是 media 被清空', 'This does not mean media was erased'), row('ANA FID05h', 'Get 的 Inaccessible／Persistent Loss／Change 受限', 'Get is restricted in Inaccessible/Persistent Loss/Change', '使用對應 ANA status', 'Use the corresponding ANA status'), row('Write Exclusive / Exclusive Access', '非 holder：前者允許 read-like；後者 read／write-like 都衝突', 'For nonholders, the former allows read-like commands; the latter conflicts with read-like and write-like commands', '兩者的非 holder write-like 都衝突', 'Nonholder write-like commands conflict for both'), row('Registrants Only / All Registrants', 'Write Exclusive 類允許所有人 read、registrants write；Exclusive Access 類僅 registrants read／write', 'Write Exclusive variants allow reads by anyone and writes by registrants; Exclusive Access variants allow reads/writes only by registrants', 'Copy 每個 source 用 read 權限，destination 用 write 權限', 'Copy checks each source as read and its destination as write'), row('Reservations', '分 read-like、write-like 命令查矩陣', 'Check the matrix for read-like/write-like commands', 'holder、registrant 與 type 必須一起看', 'Holder, registrant, and type all matter')],
 b('同一 SSD 的兩個 PCIe controllers 可共享 namespace。Controller 1 的路徑可用，仍可能因 reservation 類型與自身 registration 狀態而無法 Write；Read 是否允許需另外查表。', 'Two PCIe controllers of one SSD may share a namespace. Controller 1 can have an accessible path yet be unable to Write due to reservation type and its registration state; Read permissions require a separate matrix lookup.'),
 b('不能僅憑存在 Reservation 就一律封鎖所有非 holder 的 I/O；Write Exclusive 與 Exclusive Access 等類型的讀取規則不同。', 'A reservation does not universally block all nonholder I/O; types such as Write Exclusive and Exclusive Access have different read rules.'), [141,199], 'Namespace|ANA state|Reservation type|Holder and registrant|Command permission')

lesson('MIGRATION-QUEUE', b('LBA Migration Queue 與變更追蹤', 'LBA Migration Queue and change tracking'), '4.1.8; 5.7', '113-114,162-164',
 b('Track Send 啟用 LBA Migration Queue 後，controller 可聚合邏輯 block 修改或 deallocation 記錄。Entry 只在所報命令效果生效後發布，可早於該命令 CQE；queue entry 與 I/O completion 是不同時間點。', 'After Track Send enables an LBA Migration Queue, the controller may aggregate logical-block modifications or deallocations. Entries are posted only after the reported effects take place and may precede the command CQE; queue entry and I/O completion are distinct milestones.'),
 b('這個 queue 保存變更範圍與序列標記，不保存完整新資料。Host 讀 entry 後仍需以適當 I/O 取得資料，並處理滿 queue 的停止邊界。', 'The queue records changed ranges and sequence markers rather than complete new data. The host still retrieves data with appropriate I/O and handles the logging-stop boundary caused by a full queue.'),
 [row('LBACIR', '00：range；01：整個 namespace；10：無 range', '00: range; 01: whole namespace; 10: no range', '先判斷欄位是否有效', 'Determine field validity first'), row('ESA', '001 start／resume；010 stop；011 suspend；111 full', '001 start/resume; 010 stop; 011 suspend; 111 full', 'full 表示 logging 已停止', 'Full means logging stopped'), row('DLBA / CDQP', 'deallocated 標記／entry phase', 'Deallocation flag / entry phase', 'DLBA=0 仍可能描述 deallocate 類修改', 'DLBA=0 may still describe a deallocation-related change'), row('RALBAS', '開始命令處理期間的變更可由 ATYPE02h 補齊', 'ATYPE02h covers changes during the start command when supported', 'start／stop marker 不要求先於 Track Send CQE', 'Start/stop markers need not precede Track Send CQE')],
 b('三筆連續 Write 可合併成一個 range entry；所以 queue entry 數不等於寫入命令數。ESA=111b 後，host 不能假設後續每次修改仍持續記錄。', 'Three sequential Writes may become one range entry, so entry count is not write-command count. After ESA=111b, the host cannot assume subsequent changes continue to be logged.'),
 b('此處教學為 NVM 的 LBA 變更追蹤；不藉此展開其他傳輸協定或資源匯出格式。重新開始追蹤前要處理 queue 容量與資料一致性。', 'This lesson covers NVM LBA change tracking without expanding into other transport protocols or resource-export formats. Address queue capacity and consistency before restarting tracking.'), [194], 'Track Send|Changes take effect|Range entries|Phase and sequence|Full or stop')

lesson('SANITIZE', b('Sanitize 與 Media Verification 的 NVM 規則', 'NVM rules for Sanitize and Media Verification'), '4.1.7; 5.12', '113,173-175',
 b('NVM Sanitize 命令採 Base 定義，背景 operation 的資料語意由 §5.12 補充。成功後 Block Erase 回值由廠商定義、Crypto Erase 回值不確定、Overwrite 依 pattern 規則；若 block 已 deallocate，則改用 deallocated-read 規則。', 'The NVM Sanitize command follows Base, with §5.12 adding data semantics for the background operation. After success, Block Erase returns vendor-defined values, Crypto Erase indeterminate values, and Overwrite follows its pattern rules; deallocated blocks instead follow deallocated-read rules.'),
 b('先辨識 target、operation state 與 allocation，再決定可以驗證什麼。命令 CQE 與 sanitize operation 完成分別由不同證據表示。', 'Identify target, operation state, and allocation before deciding what can be verified. The initiating CQE and completion of the sanitize operation require different evidence.'),
 [row('LID81h', '追蹤 operation status／progress', 'Tracks operation status/progress', '啟動命令成功不是 operation 完成', 'Successful initiation is not operation completion'), row('Error Information', 'sanitize 期間 NVM LBA 欄位回 0', 'The NVM LBA field returns zero during sanitize', '僅此 NVM 補充，仍須遵守 Base command allowlist', 'This NVM extension still obeys the Base command allowlist'), row('Media Verification Read', 'PRCHK=000b、STC=0', 'PRCHK=000b, STC=0', '要求 checking 則 Invalid Field in Command', 'Requesting checking gives Invalid Field in Command'), row('Allocated media', '可讀則回實際資料；不可讀則錯誤', 'Return actual data if readable; error if unreadable', '符合條件回 Successful Media Verification Read', 'Qualifying reads return Successful Media Verification Read')],
 b('Media Verification state 中，Read 不要求 PI checking 且 allocated media 可讀時，可以忽略讀得出資料的 integrity error 並回特定成功狀態；同一 LBA 連續讀值仍 may 不同。不能用平常 Read 的固定值假設評估它。', 'In Media Verification, a Read without PI checking can ignore integrity errors when allocated media remains readable and return the specific success status. Repeated reads of one LBA may differ, so ordinary stable-value assumptions do not apply.'),
 b('Sanitize 之後讀零不是充分的成功證據，也不是所有方法都應回零。Namespace sanitize 與 subsystem sanitize 的 scope 不可互換。', 'Zero-filled reads after sanitize are not sufficient proof of success, and not every method is expected to return zeros. Namespace and subsystem sanitize scopes are not interchangeable.'), [200,201], 'Target and state|LID81h|Allocation|Verification Read|Result interpretation')

lesson('KEY-PER-IO', b('Key Per I/O 的 NVM 對齊約束', 'NVM alignment constraints for Key Per I/O'), '5.5; 4.1.5', '91-92,105,160',
 b('Key Per I/O 的 NVM 補充要求使用 key tag 的命令符合 KPIODAAG 的 LBA 起點對齊與長度 granularity；不符合時回 Invalid Field in Command。Capability、namespace enablement 與 key 管理是不同層次。', 'The NVM Key Per I/O extension requires key-tagged commands to meet KPIODAAG starting-LBA alignment and length granularity; violations return Invalid Field in Command. Capability, namespace enablement, and key management are separate layers.'),
 b('先從 KPIOCAP 與 namespace status 判斷適用，再解讀 CETYPE／CEV 的 command extension。金鑰建立及管理不由這份 NVM 補充完整定義。', 'Determine applicability from KPIOCAP and namespace status before decoding the CETYPE/CEV command extension. This NVM supplement does not fully define key creation or management.'),
 [row('KPIOCAP', '支援與 subsystem／namespace scope', 'Support and subsystem/namespace scope', '不能只看單一 enable bit', 'Do not rely on an enable bit alone'), row('KPIOSNS / KPIOENS', 'namespace 支援／啟用', 'Namespace support / enablement', '未支援時 enable 必須為 0', 'Enable must be zero without support'), row('KPIODAAG', '0-based logical-block granularity', 'Zero-based logical-block granularity', '起點及長度都必須符合', 'Both start and length must conform')],
 b('raw KPIODAAG=7 代表 8-block granularity。SLBA=16、length=8 符合；SLBA=17 或 length=7 都不符合，即使其他 PI 欄位完全正確也不能使用。', 'Raw KPIODAAG=7 represents eight-block granularity. SLBA=16 and length=8 fit; SLBA=17 or length=7 do not, even when all other PI fields are correct.'),
 b('Invalid Key Tag 與 alignment 的 Invalid Field in Command 需要不同調查方向；不能將 key-tag 拒絕都歸類為 PI checksum 錯誤。', 'Invalid Key Tag and alignment-related Invalid Field in Command require different investigations; key-tag rejection is not necessarily a PI checksum error.'), [], 'Capability|Namespace enablement|CETYPE and CEV|KPIODAAG|Status')

lesson('STREAMS', b('Streams 的 NVM 單位與優先順序', 'NVM units and priorities for Streams'), '5.2.2.3; 5.13', '128-129,175',
 b('NVM 的 Stream Write Size 以 logical blocks 表示；stream granularity length 是 SGS 乘解碼後 SWS blocks。使用 Streams 時，host 宜依 SGS／SWS 的建議處理 write／deallocate，再協調 namespace hints。', 'NVM Stream Write Size is expressed in logical blocks. Stream granularity length is SGS multiplied by decoded SWS blocks. When using Streams, hosts should follow SGS/SWS guidance for writes/deallocation and reconcile namespace hints.'),
 b('用兩層大小模型解釋 Stream Write Size 與較大的 stream granularity。它們可能和 namespace hints 成整數倍，但規格不保證每個 namespace 都如此。', 'Use two size levels: Stream Write Size and the larger stream granularity. They may be integer multiples of namespace hints, but this is not guaranteed for every namespace.'),
 [row('SWS', '建議寫入大小，以 blocks 計', 'Recommended write size in blocks', '宜為 NPWG 的倍數', 'Should be a multiple of NPWG'), row('SGS × SWS', 'stream granularity 的長度', 'Length of the stream granularity', '適用 stream deallocate 對齊／長度', 'Guides stream deallocate alignment/length'), row('Priority', '用 Streams 時優先 Streams attributes', 'Prefer Streams attributes when Streams are used', '未使用則用 namespace hints', 'Otherwise use namespace hints')],
 b('解碼後 SWS=8 blocks、SGS=4，stream granularity 是 32 blocks。8-block Write 可符合 SWS，但一個完整 granularity-unit 的 deallocate 長度是 32 blocks。', 'Decoded SWS=8 blocks and SGS=4 give a 32-block stream granularity. An eight-block Write can meet SWS, while deallocating one full granularity unit uses 32 blocks.'),
 b('Streams hints、FDP placement 與 namespace atomicity 是不同概念；不能只因尺寸相等就推論是同一個內部媒體單位。', 'Streams hints, FDP placement, and namespace atomicity are different concepts; equal sizes do not prove they identify the same internal media unit.'), [152], 'SWS blocks|SGS multiplier|Stream granularity|Namespace hints|Workload')

lesson('EXPORT-TEMPLATE', b('Memory-based 資源匯出範本', 'Memory-based resource export template'), '5.4.1-5.4.1.1', '152-159',
 b('Reference Exported NVM Subsystem Template 是選用的 memory-based 範本，限定一個 controller（ID 0h）及一個 namespace（ID 1h）。建立時必須設 TR=1；configuration state 只能以一筆命令設定一次，重複設定回 Command Sequence Error。', 'The optional Reference Exported NVM Subsystem Template is memory-based and permits one controller (ID 0h) and one namespace (ID 1h). Creation requires TR=1. Configuration state is set once in one command; repeated setting returns Command Sequence Error.'),
 b('範本固定的相容性介面與 underlying 裝置能力是兩層。先確認 namespace 格式相容，再設定不超過 underlying 能力的限制；沒有被範本開放的能力不能自行宣告。', 'The template compatibility interface and underlying device capabilities are separate layers. Check namespace-format compatibility, then configure limits no greater than underlying capabilities; do not advertise capabilities the template does not permit.'),
 [row('Identity and version', 'CAP.CSS=1、VS=020300h；NVM VER=010200h', 'CAP.CSS=1, VS=020300h; NVM VER=010200h', '範本固定 Base 2.3／NVM 1.2，不隨本 PDF 版本自動升級', 'The template fixes Base 2.3/NVM 1.2; the enclosing PDF revision does not upgrade it'), row('Controller limits', 'MDTS、RAB、NCQS、NSQS、MQES、AWUN／AWUPF 受 underlying 限制', 'MDTS, RAB, NCQS, NSQS, MQES, AWUN/AWUPF are bounded by underlying support', 'NCQS／NSQS 是 0-based', 'NCQS/NSQS are zero-based'), row('Namespace compatibility', 'LBAF0 的 LBADS／MS 必須相同，MS=0；DPS／KPIOENS／CWP 必須為零', 'LBAF0 must match underlying LBADS/MS, with MS=0; DPS/KPIOENS/CWP must be zero', 'controller 負責 Format Index remapping', 'The controller handles Format Index remapping'), row('Observable defaults', 'Error entries 與 SMART 為零；firmware active slot=1', 'Error entries and SMART are zero; firmware active slot is 1', '支援清單、Feature defaults 與 Identify exceptions 另有固定規則', 'Support lists, Feature defaults, and Identify exceptions have fixed rules')],
 b('Underlying NCQS=7 可支援 8 queues，範本設定 NCQS=3 表示最多 4 queues；不能把 raw 3 誤解成 3 queues。Namespace 的 NGUID 為零時，NUUID 必須提供有效 UUID。', 'Underlying NCQS=7 supports eight queues; configured NCQS=3 permits four queues, not three. A zero namespace NGUID requires a valid NUUID.'),
 b('Figure 190 的 caption 寫 Controller，但 payload 是 namespace configuration；依 ENSID、LBAF0、NGUID 與 NUUID 判讀。Figure 189 僅教核准的本機欄位；其餘身分欄位不展開。', 'Figure 190 says Controller in its caption, but its payload is namespace configuration: decode ENSID, LBAF0, NGUID, and NUUID. Figure 189 teaches the permitted local fields only; other identity fields are not expanded.'), list(range(183,191)), 'Underlying capability|Template and TR|IDs 0 and 1|Set once|Reported interface')

lesson('EXPORT-STATE', b('匯出狀態的長度與一致性', 'Exported-state length and consistency'), '5.4.1.2', '159-160',
 b('Reference Exported NVM Subsystem State 保存目前 Feature values 與 controller state。CSATTR.CP=1 表示整段 Migration Receive 處理期間 controller 都處於 suspended；NVMECSS 以 dwords 指出可變 NVMECS 長度，內層 VER 固定為 1h。', 'Reference Exported NVM Subsystem State stores current Feature values and controller state. CSATTR.CP=1 means the controller remained suspended throughout Migration Receive processing. NVMECSS gives the variable NVMECS length in dwords; the nested VER is fixed to 1h.'),
 b('先讀固定 64-byte header，再檢查可變長度與 suspension 證據。Configuration state 與執行中 state 的用途和設定限制不同，不能共用 payload parser。', 'Read the fixed 64-byte header, then validate variable length and suspension evidence. Configuration state and runtime state differ in purpose and setting constraints; do not share their payload parsers.'),
 [row('Feature values', '保存 arbitration、power、temperature、error recovery、queues、interrupt、atomicity 與 AEC', 'Saves arbitration, power, temperature, error recovery, queues, interrupt, atomicity, and AEC', '是 current values，不是 Figure 187 defaults', 'These are current values, not Figure 187 defaults'), row('CSATTR.CP', '1 表示整段處理期間 suspended', '1 means suspended throughout processing', '0 不保證完全沒有 suspension', '0 does not mean there was no suspension at all'), row('NVMECSS', '總長度 = 64 + 4 × NVMECSS bytes', 'Total length = 64 + 4 × NVMECSS bytes', '0 時 NVMECS 欄位不存在', 'NVMECS is absent when zero')],
 b('NVMECSS=16 時，NVMECS 有 64 bytes，整個結構有 128 bytes；先檢查乘法、加法與接收 buffer bounds，再解碼內層 VER。', 'With NVMECSS=16, NVMECS occupies 64 bytes and the complete structure 128 bytes. Check multiplication, addition, and receive-buffer bounds before decoding nested VER.'),
 b('這是 memory-based controller state 的教學；沒有完整 state 與 suspension 證據時，不從單一 Feature value 推論整個 subsystem 已安全移轉。', 'This describes memory-based controller state. A single Feature value cannot establish successful subsystem migration without complete state and suspension evidence.'), [191], 'Current features|CP evidence|NVMECSS dwords|Bounds|Nested VER')

MODULES = [dict(id='nvmcs-'+x['key'].lower(), title=x['title'], lead=x['lead'], nodes=x['nodes'],
                rows=x['rows'], example=x['example'], pitfall=x['pitfall'],
                sources=[PREFIX+'-'+x['key']], figures=x['figures']) for x in LESSONS]


def install(reports, titles, modules, glossaries):
    claims=[]
    for x in LESSONS:
        claims.append(dict(key=x['key'], section=x['section'], printed_pages=x['pages'], pdf_pages=x['pages'],
                           zh_tw=x['claim']['zh'], en=x['claim']['en'], normative_keyword='none',
                           source_id=NVM, scope_entry_id='NVMCS13-INCLUDE'))
        titles[PREFIX+'-'+x['key']] = (x['title']['zh'], x['title']['en'])
    reports[REPORT_ID] = dict(prefix=PREFIX, title_zh='NVM Command Set 1.3：完整獨立教學與工程查詢',
        title_en='NVM Command Set 1.3: Complete Independent Course and Engineering Reference',
        source_id=NVM, supporting_source_ids=[BASE], scope_entry='NVMCS13-INCLUDE',
        date='2026-09-03', verified_date='2026-09-03',
        range='NVM Command Set 1.3 第 1–5 章與 Appendix A，以及必要 Base 相依圖表；各節只保留本機／PCIe 與傳輸無關的共同語意，包含 §5.4 的 memory-based 範本',
        range_en='NVM Command Set 1.3 Chapters 1–5 and Appendix A, with required Base figure dependencies. Sections retain local/PCIe and transport-independent semantics, including the memory-based template in §5.4',
        diagram=['Namespace and format', 'Command and limits', 'Protection and placement', 'Completion and evidence'],
        diagram_note_zh='先辨識 namespace 與資料格式，再建立命令；依支援能力套用完整性、配置與管理規則，最後以 CQE、Identify、log 與事件查核結果。',
        diagram_note_en='Identify the namespace and format before constructing commands; apply supported integrity, placement, and management rules, then verify through CQEs, Identify, logs, and events.', claims=claims)
    modules[REPORT_ID]=MODULES
    glossaries[REPORT_ID]=[(term, PREFIX+'-'+key) for term,key in [
        ('LBA','FOUNDATION'),('NSZE','CAPACITY'),('NCAP','CAPACITY'),('NUSE','CAPACITY'),
        ('AWUN','ATOMIC'),('AWUPF','ATOMIC'),('NLB','READ-WRITE'),('PRACT','PI-CHECKING'),
        ('PRCHK','PI-CHECKING'),('STC','PI-CHECKING'),('STS','PI-FORMATS'),('LBADS','FORMAT-LIST'),
        ('NLBAF','FORMAT-LIST'),('NULBAF','FORMAT-LIST'),('LBAFEE','FORMAT'),('DULBE','DEALLOC'),
        ('DMRL','DSM'),('FCO','COPY'),('NSZ','ZERO-UNCORRECTABLE'),('LBACZ','ZERO-UNCORRECTABLE'),
        ('ATYPE','LBA-STATUS'),('CMPC','LBA-STATUS'),('BWSF','RATE-CONFIG'),('GC','RATE-GRAPH'),
        ('ESA','MIGRATION-QUEUE'),('KPIODAAG','KEY-PER-IO'),('SWS','STREAMS')]]
